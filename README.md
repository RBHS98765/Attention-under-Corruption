# Attention-under-Corruption
Controlled comparison of SE, BAM, and CBAM attention modules under common image corruptions. Trained on clean CIFAR‑10 with a matched CNN backbone; evaluated on CIFAR‑10‑C across corruption severities to measure how attention degrades as image quality drops.
